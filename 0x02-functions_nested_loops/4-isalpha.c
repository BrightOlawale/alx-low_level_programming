#include "main.h"

/**
* _isalpha - checks if letter is lowercase
* @c: converts to ascii
* Return: 1 if lower case
*/

int _isalpha(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else if
	{
	if (c >= 'A' && c <= 'Z')
		return (1);
	}
	else
	{
		return (0);
	}
}
